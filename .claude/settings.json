{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "jq -nc --argjson o '{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"RTFM MANDATE (AGENTS.md + CLAUDE.md). For a turn that touches a design decision, mechanism, milestone or campaign — not for a short factual or conversational turn: (1) read docs/campaigns/README.md and the campaign covering the work; (2) check docs/campaigns/research-*.md for prior art already surveyed; (3) if an external project is cited read its SOURCE, not only its paper (~/src/FreeToken-ref, ~/src/ninfer); (4) state the EVIDENCE CLASS (paper|code|measured-here) of every disposition; (5) a CONFIRMED/DEVIATION/UNSUPPORTED label is not evidence; (6) a negative that was never tried is not verified; (7) GPU work: docs/sop-card-window.md first; (8) \\\"IT DOES NOT FIT\\\" IS NOT A FINDING — before calling a capability blocked by a hardware limit, name who runs it anyway and on what, and state the NARROW blocker, never the broad one. Answer what was asked, at the length it was asked at.\"}}' '$o'",
            "timeout": 10,
            "statusMessage": "RTFM check"
          }
        ]
      }
    ]
  }
}
